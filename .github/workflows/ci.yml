name: CI

on:
  pull_request:
  push:
    branches: [main]

# Read-only by default. Nothing in this workflow writes to the repository.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and check
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      # RULE 1. This runs first and independently of the build: if roster data
      # is in the tree, the run fails before anything else is worth knowing.
      - name: No roster or member data files
        run: node scripts/check-no-data-files.mjs

      # RULE 4. The site is live, so an unfilled placeholder is no longer a
      # reminder to the pack; it is a yellow TODO box in front of a parent.
      - name: No unfilled placeholders
        run: node scripts/check-no-placeholders.mjs

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build
